<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="logo-container">
            @if (TempData["Confirm"] != null)
            {
                <div class="alert alert-success text-xxl-center">
                    <span class="text-center">@TempData["Confirm"]</span>
                </div>
            }
        </div>
    </div>
</div>